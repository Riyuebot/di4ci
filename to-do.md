# yasumiki 当前待办

## 高优先级

- [x] 对齐 `变量更新规则` 和 `schema` 里的女性角色关系字段。
  现在 `变量更新规则` 仍把核心女性角色统一写成 `好感 / 信任 / 欲望 / 深层关系阶段` 这一套，但真实 `schema` 不是这样：
  - 芹泽千枝实：`好感 / 信任 / 欲望`
  - 回末李花子：`好感 / 信赖 / 色诱度`
  - 马宫久子：`好感 / 合作度 / 色气值`
  - 织部香织：`好感 / 压抑值 / 顺从度`
  - 卷岛春：`好感 / 依赖 / 异常值`
  - 咩子：`好感 / 依赖 / 特殊性`
  这会导致 AI 没被正确教会要更新哪些真实字段，也会间接影响 EJS 阶段文案的切换效果。

## 中优先级

- [x] 决定 `角色阶段控制器` 是否继续“每次都加载 6 个女性阶段条目”。
  现在它会在非第 0 楼时固定加载：
  `千枝实阶段 / 李花子阶段 / 春阶段 / 久子阶段 / 香织阶段 / 咩子阶段`
  这样能工作，但提示词会偏厚，也可能让没出场的角色持续影响 AI。
  需要决定是否改成按在场角色、路线或近期互动按需裁剪。

- [x] 决定 `美佐峰美辻` 是否要进入正式可见角色链路。
  目前她已经存在于：
  - `schema`
  - `initvar`
  - 场景/宴会规则
  但她缺少：
  - 单独的角色世界书文件
  - `在场角色控制器` 里的 `getwi(...)` 注入入口
  - `角色详情` 中的对应描述
  如果她未来只当隐藏干涉位，可以暂时不补。
  如果她未来要正式出场，这一整条链路都得补齐。

## 规范层待确认

- [x] 确认 `[initvar]变量初始化勿开` 是否继续保留现在的配置。
  按当前项目，它放在“角色定义之前”也能跑。
  但按知识库的 MVU 自查模板，它更偏向 D 齿轮深度 4。
  这是规范差异，不是当前必炸的问题。

- [x] 确认 `变量列表` 标签名以后以哪一版为准。
  当前项目统一以 `fenjue/MVU_ZOD指南.md` 的单数版为准：
  - `<status_current_variable>`
  之前知识库里曾同时出现单数版和复数版。
  现在已经统一口径，后续都按单数版处理。

- [x] 确认 `变量输出格式` 是否继续保留当前“加强版规则”。
  这条已经定稿：不再保留加强版规则。
  当前项目里的 `变量输出格式.yaml` 已经回到知识库对应的最简固定模板。
  也就是说，这里现在以规范一致性为准，不再把项目专用约束继续塞在这个条目里。

- [x] `getwi()` 控制器架构本身没问题：控制器启用、阶段条目禁用，这套写法符合知识库。
- [x] 六个单角色阶段 EJS 已补 `@@private`，并已改成读取真实存在的条件。
- [x] `角色阶段.yaml` 总表已删除，避免再和实际生效链路混淆。
- [x] `变量列表` 已恢复为 `{{format_message_variable::stat_data}}` 直出。
- [x] 状态栏当前是事件驱动为主、轮询兜底，不再是纯轮询。
- [x] “夜间出事者只能是现有 16 人之一”的限制已经加到规则层和脚本层。
